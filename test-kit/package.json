{
  "name": "figma-sync-test-kit",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build-tokens.js",
    "preview": "node build-tokens.js && open preview.html"
  },
  "devDependencies": {
    "style-dictionary": "^4.0.0"
  }
}
