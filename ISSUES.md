# Known Issues

## Resize behavior is buggy
The `useWindowResize` hook from `@create-figma-plugin/ui` produces janky resize behavior. Needs investigation — may be a min/max constraint issue or a conflict with the Tabs layout.
